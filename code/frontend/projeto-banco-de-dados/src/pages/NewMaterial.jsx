import MaterialForm from "../components/MaterialForm";
import Page from "../components/UI/Page";

const NewMaterialPage = (props) => {
  return (
    <Page title="Cadastrar Material">
      <MaterialForm />;
    </Page>
  );
};

export default NewMaterialPage;
